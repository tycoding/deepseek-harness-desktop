/**
 * Browser surface plugin, node half. The browser implementation is exported
 * from `./client` and follows the client plugin lifecycle.
 */

/** Host plugin body; this package has no host-side behavior. */
export function apply(): void {}
